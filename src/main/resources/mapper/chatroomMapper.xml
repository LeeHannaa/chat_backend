<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chatroomMapper">
    <select id="findByPhoneNumber" parameterType="String" resultType="chatRoom">
        select id, name, memberNum, isGroup, phoneNumber, regDate, apt  from chatRoom WHERE phone_number = #{phoneNumber}
    </select>

    <insert id="save" parameterType="chatRoom" useGeneratedKeys="true" keyProperty="id">
        insert into chatRoom(name, member_num, is_group, phone_number, reg_date, apt_id)
        values(#{name}, #{memberNum}, #{isGroup}, #{phoneNumber}, #{regDate}, #{aptId})
    </insert>

    <select id="findById" parameterType="Long" resultType="chatRoom">
        select id, name, member_num, is_group, phone_number, reg_date, apt_id from chatRoom WHERE id = #{id}
    </select>
    <delete id="deleteById" parameterType="Long">
        delete from chatRoom where id = #{id}
    </delete>

</mapper>